/**
 * Defines the complete set of supported provider identifiers.
 * This serves as the single source of truth for provider types across the application.
 */
export type Provider = 'openai' | 'gemini' | 'azure' | 'grok' | 'openai-compatible';